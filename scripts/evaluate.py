
from typing import List
from argparse import ArgumentParser

# def create_parser() -> ArgumentParser:
#     parser = ArgumentParser(description="ShEx Validation Tool")
#     parser.add_argument(-)
#
#
# def validate_rdf_graph(argv: List[str]) -> bool:
#     opts = genargs(argv)